<h1 class="top-header">Here are all of the stylists:</h1>
<div class="card-block">
  <% @stylists.each do |stylist| %>
    <ul>
      <li><a href= <%= "stylists/#{stylist.id}" %> ><%= stylist.name %></a></li>
    </ul>
  <% end %>
</div>
